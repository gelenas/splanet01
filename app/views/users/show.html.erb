<div class="jumbotron">
<div class="container">
	<% provide(:title, @user.name) %>
	<div class="row">
  		<div class="col-xs-6 col-md-4">
      		<h1>
        	<%= @user.name %>
        	<%= @user.email %>
      		</h1>
  		</div>
	</div>
</div>
</div>

